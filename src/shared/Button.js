import styled from "styled-components"

export default styled.button`
  background-color: var(--bulbasaur-green);
  border-color: var(--black);
  border-top-right-radius: 5px;
  color: var(--white);
`